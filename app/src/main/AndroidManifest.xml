<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="shoping.softices.com.trainapp">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/camera"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/camera"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".splashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NavigationDrawerActivity"
            android:label="@string/title_activity_navigation_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SigninActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".ForgetActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".DashboardActivity" />
        <activity
            android:name=".RecycleviewActivity"
            android:label="@string/title_activity_recycleview"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Userlist" />
        <activity android:name=".BatteryIndicatorActivity" />
        <activity android:name=".AllContactActivity" />
        <activity android:name=".SingleContactActivity"></activity>
    </application>

</manifest>